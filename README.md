## Global configs:

- defaults
  - aws
    - region
    - s3Bucket
    - cfDistributionId
    - credentials (?)
      - accessKeyId (?)
      - secretAccessKey (?)

## Project configs

- client
- project
- distFolder
- public
- username
- password
- aws
  - region
  - s3Bucket
  - cfDistributionId
  - credentials (?)
    - accessKeyId (?)
    - secretAccessKey (?)

### To-do

- aws
  - region
  - s3Bucket
  - cfDistributionId
  - credentials (?)
    - accessKeyId (?)
    - secretAccessKey (?)
